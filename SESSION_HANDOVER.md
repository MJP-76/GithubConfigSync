# Session handover

Use `E:\NextCloud\_Projects\GitHub\GithubConfigSync\SESSION_HANDOVER.md` as the shared guide for setup, stopping, and resuming work on another device.

## Current snapshot

- Release line: `0.3.2` dev
- Branch: `dev`
- Central repo path: `E:\NextCloud\_Projects\GitHub\GithubConfigSync`
- Latest fixes: clean-repo now uses recursive deletes, dry-run planning is always visible, `.ha_run.lock` is ignored, and the repo is aligned to `0.3.2`.

## Resume order

1. Read `PROJECT_PLAN.md`.
2. Read `SESSION_SHORTCUTS.md`.
3. Read `SESSION_MULTI_DEVICE.md`.
4. Fetch and continue on `dev`, then keep the tracker aligned with the forked prerelease version line.
